# File Association Proxy (for Windows)
A file association proxy handler for Windows, that allows for more fine grain control over how associated applications are launched compared to standard "Open With"

### TODO: Write Readme
